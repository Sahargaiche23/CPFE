server:
  port: 8081

spring:
  application:
    name: employer-service
  codec:
    max-in-memory-size: 50MB
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
  jackson:
    mapper:
      accept-case-insensitive-enums: true
    parser:
      allow-comments: true
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: COPT
    password: copt123
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui/index.html

# URLs des services internes (Docker)
auth.service.url: http://cooperation-auth:8089
notification.service.url: http://cooperation-notification:8087
