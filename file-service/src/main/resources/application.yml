server:
  port: 8088
spring:
  application:
    name: file-service
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: COPT
    password: copt123
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
file:
  upload-dir: /var/cnss/uploads

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui/index.html
